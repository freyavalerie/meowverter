# Rust / Tauri build output
/src-tauri/target/
**/target/

# Bundled installers + updater artifacts (published to GitHub Releases, not the repo)
/src-tauri/target/release/bundle/

# Signing keys - NEVER commit these (they live in ~/.catverter/ anyway)
*.key
*.key.pub
updater.key*

# OS / editor cruft
Thumbs.db
.DS_Store
*.log
